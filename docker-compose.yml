services:
  windows-lab:
    build: .
    image: msjpq/windows-in-docker:latest
    container_name: windows-lab
    environment:
      - TZ=Europe/Paris
      - DATA_DIR=/data
      - DEV=1
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
